/**
 * resilient-pubsub/idempotency
 *
 * Idempotency store abstractions for deduplication of published messages.
 *
 * Future exports:
 * - IdempotencyStore: interface for pluggable backend stores
 * - InMemoryIdempotencyStore: in-process store for testing / single-instance use
 * - IdempotencyOptions: configuration for TTL, key derivation
 *
 * @module idempotency
 */

/**
 * Placeholder export — implementation lands in a future PR.
 *
 * @internal
 */
export const _idempotencyVersion = '0.0.0' as const;
